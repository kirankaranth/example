
from setuptools import setup, find_packages
setup(
    name = 'ExampleExt',
    version = '0.0.1',
    packages = ['ExampleExt'],
    package_dir = {'ExampleExt': '.'},
    description = '',
    install_requires = [],
)

